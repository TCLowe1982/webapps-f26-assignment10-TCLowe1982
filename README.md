# webapps-f26-assignment10-TCLowe1982
Web Apps Assignement 10

I think I screwed up the repo name. Oh well.

1. First you are going to decide on a class of resource.  Use the last digit of your ID to determine the initial letter you will use.  You will determine a class of items where the initial letter of the class is determined by the table.  There should be multiple instances possible for the class. The class should have at least three attributes including a number and a string.  As an example, if 3 is my last digit I can select as my class Costumes with attributes costume_type (string), size (string), and cost (number).  You will not use Costumes. You can use at most one of the attributes I used.  Be different. Your kind of item should not be something that you can typically buy on Amazon. (Avoid using “type” as the name of an attribute as that may cause problems later.)
Inital letter is B (2).
Class is Bombs (I was going to do baskets, but you can buy those on Amazon).
Attribute Type is weight(float), yeild(float), and reaction(string)
